# Contract & index HQ for the suite, plus the one shared UI crate (crates/suite-ui).
/target
.DS_Store
Thumbs.db

# Release artifacts produced by release tooling
/dist
# Compiled installer binary (build artifact)
/linux-ops-install
