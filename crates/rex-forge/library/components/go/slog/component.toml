name = "slog"
language = "go"
category = "Observability"
summary = "Stdlib structured logging (log/slog)"
bases = ["go-bin"]

[[inject]]
target = "main.go"
anchor = "rex:imports"
template = "files/inject/imports.go.j2"

[[inject]]
target = "main.go"
anchor = "rex:init"
template = "files/inject/init.go.j2"

[[note]]
text = "Uses the stdlib log/slog JSON handler on stderr."
