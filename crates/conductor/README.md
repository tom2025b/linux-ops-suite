# conductor

The Linux Ops Suite's **guided operator** (Phase 1: read-only foundation).
Reads the suite's own state files, derives a short **ordered runbook**, and
prints it. Full usage lands in Task 10. See `CONDUCTOR_DESIGN.md` at the repo
root.
