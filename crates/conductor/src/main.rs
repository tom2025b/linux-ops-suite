//! conductor CLI — stub. Replaced by the full thin-CLI dispatch in Task 9; this
//! placeholder exists only so the `[[bin]]` target compiles while the library is
//! built up task by task.

fn main() {
    eprintln!("conductor: CLI not wired yet (Phase 1 in progress)");
}
